# Changelog

## [0.4.3] - 2026-09-15

### Added
- **监控面板第二行可自定义**：设置面板新增「第二行内容」区，总开关 + CPU/内存/GPU/网络四列独立下拉。
  - CPU 可选：型号 `i9-10900K`、核心高低占用、`10C 20T`、进程数、主板型号。
  - 内存可选：类型+频率 `DDR4 2133`、频率、插槽 `4×16G`、内存型号、主板型号。
  - GPU 可选：型号 `RTX 2080 Ti`、温度·功耗、核心/显存频率、温度·风扇、主板型号。
  - 网络可选：累计用量、本机 IP、链路速率、主板型号。
- **监控静态缓存扩展**：首次启动即显示 CPU/内存/GPU/网卡/磁盘的硬件固定信息（型号、频率、容量、IP 等），采样到达后再换实时值。

### Changed
- **设置面板重排**：外观区把 Mini 预览、主题按钮、网速预览放在同一行；所有滑杆左侧显示当前值；冗长说明改为 `ⓘ` 悬停提示，窗口更紧凑。
- **天气顶部对齐**：天气状况文字（如「晴」）现在在温度数值下方水平居中，温度位置保持不变。

## [0.4.2] - 2026-09-15

### 监控面板：冷启动就是它自己那台机器

启动骨架屏只在**第一次运行**（磁盘上还没有缓存）时出现。之后每次启动，面板直接照着
上次记下的硬件信息画出来，只有需要当场测量的数字是 0。

- **会记住的**：CPU 型号、核心数与额定频率；内存总容量与 DDR 频率；GPU 名称与显存容量；
  网卡名；每个盘的盘符、名称、总容量与已用量；物理驱动器的组合方式（`SSD C+D`、`HDD E`）。
  这些写在 `%APPDATA%\WSight\monitor-cache.json`，首次采样写一次，之后最多每分钟刷新一次。
- **只能现测的，显示 0 或 `--`**：占用率、已用内存、已用显存、温度、网速、磁盘读写速度。
  上一小时的下载速度不是"现在"的事实，就不装成是。
- **三个圆环在等到数据之前画满一圈并淡化**，中间是 `--` 而不是 0%——满圈表示"表盘在，
  只是还没读数"，一个自信的 0% 则是它无权给出的结论。
- 三态尺寸实测一致（骨架 / 缓存态 / 实时态都是 420×306），数据落地时窗口不跳。

---

## [0.4.1] - 2026-09-15

### 修复：开机启动

- **只跑一份 WSight**。两个实例共用同一个 WebView2 用户数据目录，后启动的那份会卡在
  profile 上，画出一个空白且点不动关闭的窗口——正是"开机后任务栏冒出 WSight 设置、
  关不掉"的样子：Run 项在登录时拉起程序，用户看悬浮窗没出来又点了一次图标。
  现在第二个实例会安静退出。
- **开机启动改为延迟 5 秒**再创建窗口（Run 项写成 `WSight.exe --startup`）。登录瞬间磁盘
  和其他启动项挤在一起，WebView2 运行时也在往内存里翻，抢这一下最容易画出空窗口。
- **首运行判断收紧**：只有配置文件**不存在**时才自动打开设置窗口。以前是"城市为空就弹"，
  一次读不到配置就会在每次开机时弹一次。
- 新增 `%APPDATA%\WSight\launch.log`：每次启动记下是手动还是开机自启、有没有别的实例在跑、
  读的是哪个配置文件、城市有没有值。下次再出问题，看日志就知道，不用复现。

### 监控面板

- **启动骨架屏**：窗口一出现就先摆好整块界面，等采集器的第一份数据到了再换成真实数字。
  监控区（CPU / 内存 / GPU / 网络）和磁盘块是**两组独立的骨架**，各自跟着设置开关走——
  关掉 GPU 或网络就少一列，关掉磁盘那一整块骨架也不出现。
- 骨架只是占位，不做缓存：占位块用的是真实文字的字号和行高，所以骨架和加载完的面板
  **尺寸完全一致**（实测 420×306 两组相同），数据落地的瞬间不会跳高度。

---

## [0.4.0] - 2026-09-15

### 天气面板

- **秒开：先出画面再刷新**。上次成功获取的天气会写入
  `%APPDATA%\WSight\weather-cache.json`，冷启动时前端直接读盘渲染，界面瞬间出现
  完整卡片，右上角用一个呼吸的小圆点表示「更新中」，新数据回来后无缝替换。
- **无缓存时显示骨架屏**（首次安装或缓存过期）：占位块按真实卡片的高度与圆角绘制，
  加载完成时不会有高度跳变。
- 观测时间超过 30 分钟未更新时，时间戳文案自动置灰，提示这份数据已经不新鲜。

### 设置

- 新增**开机启动**开关（通用区）。开启后写入
  `HKCU\Software\Microsoft\Windows\CurrentVersion\Run`；关闭时删除该项。
  每次启动都会按当前配置重写一次，避免卸载后又残留。
- 预报天数范围放宽为 **3–7 天**。

### 内部

- 天气轮询的首次请求延迟由 1.2s 调整为 15s：既然冷启动已有缓存/骨架屏兜底，
  就没必要在开软件的一瞬间再打一次接口。
- 新增 `get_cached_weather` 命令与 `autostart` 模块（用 `winreg` 直接读写注册表，
  不依赖外部 `reg.exe`）。

---

## [0.3.0] - 2026-09-15

### 天气面板

- **生活指数排版重写**：文案先压平 API 原文的换行，再按标点断行（断点吃掉标点，行尾不会挂符号），
  最多 3 行、每行上限 22 字，并有 30% 下限避免第二行只剩两三个字。
- **指数区固定 3 行高度**，窗口高度不再随文案长短跳动。
- **顶栏图标与温度对齐**：所有天气图形按各自包围盒居中并统一尺寸，viewBox 收紧去掉内部留白，
  图标与「温度 + 天气」文字块中心对齐（偏差从 11px 降到约 3px），月牙图形重绘。
- **预报天数可配置 3–7 天**（原为固定 7 天），保留「一行显示 3–5 格」设置。
- 修复预报**横向滚动失效**：格子之前会被 flex 压回一行，改为固定宽度后滚动与右侧箭头均正常。
- 现状行去掉气压，改为单行：`湿度 72% · 西南风 1级 · 能见度 26km`。

### 监控面板

- **磁盘区**：标题改为「磁盘 C/D/E」，右侧读写速度改为横版 `读 X 写 Y`（不再用箭头），
  `SSD C+D` 标题缩小；整个磁盘区固定高度、内容垂直居中。
- **圆环动画流畅度**：数据每约 0.5s 才更新一次，旧版靠 CSS 过渡导致"跳一格再静止"。
  改为 rAF 插值持续驱动弧长与数字，并移除每帧重算的 `drop-shadow` 光晕。
- 新增 `debugFps` 诊断开关：开启后窗口标题显示实时帧率与采样间隔。

### 其他

- CPU 默认配色由蓝改为琥珀色（旧配置中的蓝色默认值会自动迁移）。

---

## [0.2.0] - 2026-09-14

### 监控面板重构

- **网络标题**：改为真实网卡名称（如 `以太网 2`）。
- **CPU 列**：
  - 第一行：实时睿频 / 额定频率（如 `4.79/3.70GHz`）。
  - 第二行：最高核心占用 / 最低核心占用。
- **内存列**：
  - 第一行：已用 / 总量（如 `19.1/63.8G`）。
  - 第二行：DDR 频率（如 `2133MHz`）。
- **GPU 列**：
  - 第一行：显存已用 / 总量。
  - 第二行：温度 · 功耗。
- **磁盘区**：
  - 左侧按**卷**展示容量占用（C/D/E…），最多自选 3 个，带 SSD/HDD 类型识别。
  - 右侧按**物理硬盘**展示读写速度，最多自选 2 块。
  - 去掉卷前图标，收窄容量条，拉开间距，读写速度改为整数显示。
- **移除旧面板下方的进程 / 明细区域**，整体改为四列紧凑布局。

### 天气面板

- 七格预报保持；现状行、舒适度、交通指数等维持不变。

### 设置窗口

- 新增版本信息（`v0.2.0`）。
- 新增「开源主页 / 更新日志 / 反馈问题」跳转按钮。
- 磁盘卷、物理硬盘改为逐项开关选择。
- 移除已废弃的「内存明细」「占用最高进程」开关。

### 工程实现

- 新增 `src/shared/about.ts` 与 `scripts/check-version.mjs`，统一 `package.json` / `tauri.conf.json` / `Cargo.toml` / `about.ts` 四处版本号。
- 新增 `.gitignore`、`LICENSE`（MIT）、本 `CHANGELOG.md`。

---

## [0.1.0] - 2026-09-13

### 初始版本

- 硬件监控面板（CPU、内存、GPU、网络、磁盘占用、Top 进程）。
- 和风天气面板（实况 / 7 天预报）。
- 无边框悬浮窗口、托盘管理、主题与位置设置。
